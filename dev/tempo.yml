server:
  http_listen_port: 3200
  grpc_listen_port: 4317

distributor:
  rate_limit_enabled: false

storage:
  trace:
    backend: local
    local:
      path: /var/tempo

querier:
  max_concurrent_queries: 20